import os

# ---------------------------------------------------------
# 1. إعدادات الاتصال (Green-API)
# ---------------------------------------------------------
INSTANCE_ID = "7105395235"
API_TOKEN = "7a7cf9442dbc4d9cb736b48c11ff9c5a077f22ed00fc465dbe"

# ---------------------------------------------------------
# 2. مسارات الملفات وقواعد البيانات
# ---------------------------------------------------------
# ملف نصوص القرآن (النسخة الجديدة: حزب، بدون تشكيل)
QURAN_FILE = "mainDataQuran.json"

# ملف قائمة القراء وروابط الصوت
RECITERS_FILE = "reciters.json"

# ملف حفظ إعدادات المستخدمين (جديد)
USERS_FILE = "users.json"

# المجلد الذي سنحفظ فيه الأصوات مؤقتاً أثناء عملية الدمج (المونتاج)
AUDIO_CACHE_DIR = "audio_temp"

# ---------------------------------------------------------
# 3. إعدادات الحدود والأداء
# ---------------------------------------------------------
# الحد الأقصى لطول الرسالة النصية الواحدة (حوالي 35 ألف حرف للأمان)
MAX_MESSAGE_LENGTH = 15000 

# ثواني الانتظار بين الرسائل المقسمة لضمان الترتيب
DELAY_BETWEEN_PARTS = 2

# ---------------------------------------------------------
# 4. إعدادات المونتاج الصوتي (Audio Merger)
# ---------------------------------------------------------
# أقصى عدد آيات يُسمح بدمجها في ملف واحد.
# (وضعنا 100 آية حماية للسيرفر من الانهيار إذا طلب شخص سورة البقرة كاملة دمج)
# إذا كان الطلب أكثر من هذا الرقم، سنرسل الصوت مقسماً أو كروابط.
MAX_VERSES_TO_MERGE = 100

# ---------------------------------------------------------
# 5. الإعدادات الافتراضية للمستخدم الجديد
# ---------------------------------------------------------
# أي شخص يراسل البوت لأول مرة سيحصل على هذه الإعدادات تلقائياً
DEFAULT_USER_SETTINGS = {
    "text_enabled": True,    # إرسال النص مكتوباً
    "audio_enabled": True,   # إرسال الملف الصوتي
    "reciter_id": 1,         # رقم القارئ الافتراضي (سنحدده في ملف القراء)
    "merge_audio": True      # تفعيل الدمج (المونتاج) بدلاً من الملفات المنفصلة
}

# التأكد من وجود مجلد الصوت المؤقت (إنشاؤه إذا لم يكن موجوداً)
if not os.path.exists(AUDIO_CACHE_DIR):
    os.makedirs(AUDIO_CACHE_DIR)
