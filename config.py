import os

# ---------------------------------------------------------
# 1. روابط السيرفرات (أهم جزء)
# ---------------------------------------------------------

# رابط بوت البايثون الخاص بك (هذا السيرفر)
# يُستخدم لتحويل الملفات الصوتية المدمجة محلياً إلى روابط إنترنت
MY_BOT_URL = "https://whatsapp-bot-jh7d.onrender.com"

# رابط سيرفر الواتساب (Node.js) الذي يستقبل ويرسل الرسائل
WAHA_BASE_URL = "https://surver-for-whatsapp.onrender.com"

# المفتاح (للمستقبل، حالياً قد لا نستخدمه بصرامة في السيرفر الجديد لكن خليه)
WAHA_API_KEY = "12345"

# ---------------------------------------------------------
# 2. مسارات الملفات وقاعدة البيانات
# ---------------------------------------------------------
QURAN_FILE = "mainDataQuran.json"
RECITERS_FILE = "reciters.json"
USERS_FILE = "users.json"
AUDIO_CACHE_DIR = "audio_temp"  # المجلد الذي تحفظ فيه التلاوات المدمجة

# ---------------------------------------------------------
# 3. إعدادات الأداء والإرسال
# ---------------------------------------------------------
MAX_MESSAGE_LENGTH = 35000 
DELAY_BETWEEN_PARTS = 3

# ---------------------------------------------------------
# 4. إعدادات مونتاج التلاوات
# ---------------------------------------------------------
MAX_VERSES_TO_MERGE = 100

# ---------------------------------------------------------
# 5. الإعدادات الافتراضية للمستخدمين
# ---------------------------------------------------------
DEFAULT_USER_SETTINGS = {
    "text_enabled": True,
    "audio_enabled": True,
    "reciter_id": 1,
    "merge_audio": True,
    "repeat_count": 1}

# ---------------------------------------------------------
# 6. تهيئة النظام
# ---------------------------------------------------------
# التأكد من وجود مجلد الصوتيات، وإذا لم يكن موجوداً يتم إنشاؤه
if not os.path.exists(AUDIO_CACHE_DIR):
    os.makedirs(AUDIO_CACHE_DIR)
    print(f"📁 Created audio directory: {AUDIO_CACHE_DIR}")
